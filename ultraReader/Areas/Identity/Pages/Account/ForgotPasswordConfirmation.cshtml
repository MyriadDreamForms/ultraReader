@page
@model ForgotPasswordConfirmationModel
@{
    ViewData["Title"] = "Şifre Sıfırlama Talimatı Gönderildi";
}

<div class="container mx-auto px-4 py-8">
    <h1 class="text-2xl md:text-3xl font-bold text-gray-800 dark:text-gray-200 mb-6">@ViewData["Title"]</h1>
    
    <div class="max-w-md mx-auto">
        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md p-6">
            <div class="flex items-center justify-center mb-6">
                <i class="bi bi-envelope-check text-5xl text-primary"></i>
            </div>
            
            <div class="text-center mb-6">
                <p class="text-gray-700 dark:text-gray-300">
                    Lütfen e-posta adresinizi kontrol edin. Şifre sıfırlama talimatlarını içeren bir e-posta gönderdik.
                </p>
            </div>
            
            <div class="bg-blue-50 dark:bg-blue-900/20 border-l-4 border-blue-400 dark:border-blue-600 p-4 mb-6">
                <div class="flex items-start">
                    <i class="bi bi-info-circle text-blue-500 dark:text-blue-400 mr-2 text-xl flex-shrink-0 mt-0.5"></i>
                    <p class="text-blue-700 dark:text-blue-300 text-sm">
                        Eğer e-postayı göremiyorsanız, lütfen spam klasörünüzü kontrol edin. Bazen filtreleme sistemleri e-postalarımızı spam olarak işaretleyebilir.
                    </p>
                </div>
            </div>
            
            <div class="text-center">
                <a asp-page="./Login" class="inline-flex items-center justify-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-primary hover:bg-primary-dark focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary">
                    <i class="bi bi-arrow-left mr-2"></i> Giriş sayfasına dön
                </a>
            </div>
        </div>
    </div>
</div> 